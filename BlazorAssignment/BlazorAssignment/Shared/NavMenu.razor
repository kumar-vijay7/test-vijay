<div class="top-row pl-4 navbar navbar-dark">
    <a class="navbar-brand" href="">BlazorAssignment</a>
    <button class="navbar-toggler" @onclick="ToggleNavMenu">
        <span class="navbar-toggler-icon"></span>
    </button>
</div>

<div class="@NavMenuCssClass" @onclick="ToggleNavMenu">
    <ul class="nav flex-column">
        @foreach (var item in navdata)
        {
            <li class="nav-item px-3">
                <NavLink class="nav-link" href="@item.NavLink" Match="NavLinkMatch.All">
                    <span class="oi oi-home" aria-hidden="true"></span> @item.NavMenuName
                </NavLink>
            </li>
        }

        @*<li class="nav-item px-3">
            <NavLink class="nav-link" href="counter">
                <span class="oi oi-plus" aria-hidden="true"></span> Counter
            </NavLink>
        </li>
        <li class="nav-item px-3">
            <NavLink class="nav-link" href="fetchdata">
                <span class="oi oi-list-rich" aria-hidden="true"></span> Fetch data
            </NavLink>
        </li>*@
    </ul>
</div>

@code {
    public IEnumerable<TblNavData> navdata;
    bool collapseNavMenu = true;

    string NavMenuCssClass => collapseNavMenu ? "collapse" : null;
    
    protected override void OnInitialized()
    {
        NavLoad();
        
        base.OnInitialized();
    }
    void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }

    public void NavLoad()
    {
        navdata = (navservice.GetNavDatas());
        StateHasChanged();
    }
}
